# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://user:password@localhost:5432/macrochef"

# NextAuth signing secret — used to sign JWTs and session tokens
# Generate a secure value with: npx auth secret
AUTH_SECRET="your-secret-here"
